{
  "name": "ocr-pos-app",
  "version": "1.0.0",
  "main": "backend/app.js",
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "start": "node backend/app.js"
  }
}